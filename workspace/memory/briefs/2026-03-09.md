# Morning Brief — 2026-03-09 (Monday)

## 🔴 Carry-over: Security (still unresolved)
- **Telegram** `groupPolicy="open"` + elevated tools = prompt injection risk.
  - Fix: set `allowlist` + `workspaceOnly=true` for filesystem tools.
- **iMessage** `groupPolicy="allowlist"` but `groupAllowFrom=[]` → all group messages silently dropped.
  - Fix: add sender IDs to `groupAllowFrom` or set to `"open"`.
- Status: **neither has been applied yet**. Do this first.

## 🟡 In-flight: Claude model config (incomplete)
- Oppa was setting up Anthropic API key + Claude 4.6 models.
- Correct command pattern (confirmed):
  ```bash
  openclaw config set env.ANTHROPIC_API_KEY '"sk-ant-..."'
  openclaw config set agents.defaults.model.primary '"anthropic/claude-sonnet-4-6"'
  openclaw config set agents.defaults.model.fallbacks '["anthropic/claude-opus-4-6"]'
  openclaw config validate
  ```
- Was interrupted — verify if the config was actually saved and validate is passing.

## 🟡 In-flight: Eraser MCP
- Added to mcporter but `ERASER_API_TOKEN` is still a placeholder (`<your-api-token>`).
- Needs real token from https://app.eraser.io before it's usable.

## ✅ Completed yesterday
- v3.0 SULLYOON × ELON architecture finalized and saved to shared-brain.
- 14 GWS skills confirmed installed and active.
- v3.0 Excalidraw diagram saved + pushed to GitHub.

## 📌 Next priority (after security + claude config)
- Design v3.0 implementation plan: who builds ALFRED, when does Async Mail Protocol go live?
- Consider: ALFRED cron should be added to OpenClaw scheduler once expertise.yaml structure is defined.

## 🗒️ Process note
- Daily log `memory/2026-03-08.md` was NOT created today (3rd miss in a row).
- Write `memory/2026-03-09.md` early tomorrow — not at night.
