# Morning Brief — 2026-03-05

## What matters first
1. **Close the security gap before anything else**
   - Switch Telegram groups from `groupPolicy=open` to `allowlist`.
   - Enforce `workspaceOnly=true` for filesystem tools.
   - Verify with `openclaw security audit` (don’t trust status-only checks).

2. **Stabilize kimi profile reliability**
   - Run focused root-cause pass on Discord/Google timeout spikes.
   - Capture timing patterns + likely bottleneck layer (network, provider, or gateway queueing).

3. **Logging discipline**
   - Today’s daily log file was missing at night checkpoint; create/maintain same-day log early.

## Carryover context
- No new strategic direction was added yesterday; this is an execution-risk cleanup day.
