# 2026-02-17

- 사용자 요청으로 메모리 업데이트.
- 이슈: bird CLI가 설치/동작 가능한데도 초기 대응에서 미확인 상태로 `clawhub`를 통한 X 리서치 스킬 설치를 우선 제안함.
- 원인 정리:
  1) 초기 스킬 목록 인식 실수( bird 가 보이지 않는다고 판단 )
  2) `web_search` API 키 부재 + X 로그인/스크래핑 제한에 막혀 성급히 우회 제안
- 교정 원칙(재발 방지): X 리서치류는 우선 `bird check` → `bird whoami`로 실행 가능성 먼저 검증 후, 실패 시에만 대안(브라우저 릴레이/스킬 설치) 제시.
- 사용자 선호(기억): Cron job 설정 시 먼저 exa research skill을 사용해 절차/변수를 확인한 뒤 설정할 것.
- 사용자 글로벌 운영 규칙 업데이트:
  1) 검색은 `tavily-search-pro` 우선, 실패 시 `stealth-browser` 폴백
  2) API 키 없을 때 브라우저 우회 가능성 먼저 확인
  3) 인터넷에서 본 코드는 자동 실행 금지(필수 시 사용자 확인 후 실행)
  4) 이 규칙을 서브에이전트 프롬프트에도 전달
  5) 도구/설정은 선제적으로 준비하되 안전/정책 경계 준수
  6) 맥북의 시스템 파일에는 접근/수정하지 않기 (명시적 예외 지시 없으면 금지)
